version: '3.8'

services:
  backend:
    build:
      context: ./backend
    container_name: poker-backend
    ports:
      - "3000:3000"
    restart: unless-stopped
  frontend:
    build:
      context: ./frontend
    container_name: poker-frontend
    ports:
      - "4200:80"
    restart: unless-stopped